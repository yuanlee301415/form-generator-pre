/**
 * 组件尺寸
 * @enum
 * @property {'medium'} COMPONENT_SIZE.MEDIUM medium
 * @property {'small'} COMPONENT_SIZE.SMALL small
 * @property {'mini'} COMPONENT_SIZE.MINI mini
 */
export const COMPONENT_SIZE = {
    MEDIUM: 'medium',
    SMALL: 'small',
    MINI: 'mini'
}

/**
 * 标签对齐
 * @enum
 * @property {'left'} LABEL_POSITION.LEFT left
 * @property {'right'} LABEL_POSITION.RIGHT right
 * @property {'top'} LABEL_POSITION.TOP top
 */
export const LABEL_POSITION = {
    LEFT: 'left',
    RIGHT: 'right',
    TOP: 'top'
}

/**
 * 数据类型
 * @enum
 * @property {'dynamic'} DATA_TYPE.DYNAMIC 动态数据
 * @property {'static'} DATA_TYPE.STATIC 静态数据
 */
export const DATA_TYPE = {
    DYNAMIC: 'dynamic',
    STATIC: 'static'
}
